<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:compat="http://schemas.android.com/apk/res-auto"
    tools:context="it.polimi.frontend.activity.RequestActivity" >

    <item
        android:id="@+id/insert_next"
        android:orderInCategory="100"
        android:icon="@drawable/ic_menu_forward"
        android:title="Avanti"
        android:visible="true"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/insert_cancel"
        android:icon="@android:drawable/ic_menu_close_clear_cancel"
        android:orderInCategory="100"
        android:title="Annulla"
        android:visible="true"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/insert_back"
        android:orderInCategory="100"
        android:icon="@drawable/ic_menu_back"
        android:title="Indietro"
        android:visible="false"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/insert_done"
        android:icon="@drawable/ic_menu_mark"
        android:orderInCategory="100"
        android:title="Salva"
        android:visible="false"
        app:showAsAction="ifRoom"/>
	<!--  Alternativa al 'Mark' per il salva
        android:icon="@android:drawable/ic_menu_save"
        -->
</menu>